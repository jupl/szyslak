{
  "private": true,
  "directories": {
    "app": "target"
  },
  "scripts": {
    "dev": "cd target && electron .",
    "package": "npm run build -- --dir",
    "build": "build"
  },
  "devDependencies": {
    "electron": "^1.4.11",
    "electron-builder": "^10.6.1"
  }
}
